using UnityEngine;

public class Attack : MonoBehaviour
{
    
    // Update is called once per frame
    void Update()
    {
        // if (attack key is pressed){
        // play animation
        // collider on weapon
        // when hitting enemy trigger, deal damage
        // waitforseconds, before attacking again
    //}
}
}
