using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InventorySlot : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // if OnMouseDrag on object,
        // grab object
        // LootObj can be dragged and then placed into QuickSelect
        // if OnMouseDown on object,
        // Dialogue box will open up asking to verify use of object
        // select either use or cancel
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
